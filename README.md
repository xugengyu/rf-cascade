# RF Cascade Analyzer

RF and microwave cascade analysis, tracking gain, noise figure, IP3, and power through system blocks.

## Running Locally

Open `index.html` directly in a browser, or run a local static file server:

```bash
npx serve
```
